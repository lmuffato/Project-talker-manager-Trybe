// Nessa pasta estão contidos os arquivos que se comunicam com a pasta de model e leva a resposta para a camada de controllers

const getAllTalkerService = () => {};

module.exports = { getAllTalkerService };
