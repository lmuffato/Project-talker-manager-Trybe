// const { StatusCodes } = require('http-status-codes');

// const { tokenGenerator } = require('../utilities/utilities');

// module.exports = (_req, res) => {
//   res.status(StatusCodes.OK).json({ token: tokenGenerator(16) });
//  };
